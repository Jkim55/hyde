#!/usr/bin/env ruby
require './lib/fileio'

arg0 = ARGV[0]
arg1 = ARGV[1]

puts "You ran Hyde with these arguments: #{arg0} #{arg1}."

if arg0 == "new"
  Fileio.new.create_tree_structure(arg1)
else
  puts "Error! '#{arg1}' doesn't compute! Moron!"
end
